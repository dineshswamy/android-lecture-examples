<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.komparify.activitytuts.app.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:id="@+id/text_view"
        android:layout_height="wrap_content">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Andy Rubin"
        android:textSize="20dp"
        android:gravity="left"
        android:id="@+id/founder_name"
        android:visibility="gone"
        android:layout_gravity="center"


        />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Kitkat"
            android:textSize="20dp"
            android:gravity="left"
            android:visibility="gone"
            android:layout_gravity="center"
            android:id="@+id/founding_company_name"
            android:layout_toRightOf="@id/founder_name"
            />

    </LinearLayout>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_view"
        android:text="Start activity"
        android:id="@+id/start_new_activity"
        />

</RelativeLayout>
